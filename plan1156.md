# PG-MoE 实验状态追踪 (Plan 1156)

根据 `规划0505.md` 的要求，经过与此前已执行实验的记录对比，当前项目进度如下：

## 🟢 第一部分：已跑完的实验
*这些实验对应的模型文件（.pth）和性能日志已存放于 `results/` 目录下，可以直接用于填报最终表格。*

1. **[Exp-0.1, 0.2, 0.3] PG-MoE 完整基准及重复性实验**：在 Dataset-B 上已完成 MoE+Orth(0.05) 的多次标准跑测。
2. **[Exp-0.4, 0.5] 纯单特征老基线**：DCT 基线与 LGrad 基线在原始框架阶段已完成。
3. **[Exp-1.4] Concat Fusion (拼接融合)**：在 Dataset-B 上已完成极速训练模式下的拼接对比 (`fast_concat`)。
4. **[Exp-1.5, Exp-2.2] PG-MoE (动态融合基准)**：同 Exp-0 系列。
5. **[Exp-2.1] w/o Orthogonal Loss (无正交损失消融)**：在 Dataset-B 上已完成 lambda_orth=0.0 的消融训练。

---

## 🟡 第二部分：待跑的实验 (需要修改模型训练代码)
*这些是对 `train_fast.py` 或 `model_fast.py` 内部架构需要进行微调才能跑的实验。*

1. **[Exp-1.1] Spatial only (仅空间专家)**
   - **需要修改代码**：修改 `PGMoEFast` 网络结构或前向传播，使其只保留并通过空间专家的特征直接接分类头，彻底去掉频域分支。
2. **[Exp-1.2] Frequency only (仅频域专家)**
   - **需要修改代码**：修改 `PGMoEFast`，只保留频域分支接分类头。
3. **[Exp-1.3] Add Fusion (相加融合)**
   - **需要修改代码**：扩展 `PGMoEFast` 现有的 `--fusion_type`（当前只有 moe 和 concat），新增 `add` 选项，让两个专家特征相加后进入分类头，而不经过 Router。

---

## 🔵 第三部分：待跑的评估任务 (无需重新训练，需编写新推理脚本)
*针对已训练好的最佳 `.pth` 权重，进行事后的指标计算与图表绘制，这也是后续工作的重头戏。*

1. **[Exp-2.3] 特征相似度 (Feature Similarity) 提取**
   - **代码修改**：需要编写事后脚本，加载 `moe_0.05` 和 `moe_0.00` 两个权重跑测试集，钩取专家层输出特征，计算 L2 归一化后的余弦相似度并画图。
2. **[Exp-3.X] 路由权重 (Routing Distribution) 可解释统计**
   - **代码修改**：需要编写事后脚本，加载完整模型跑测试集，按“真实、生成、分对的生成、分错的生成”四个维度截取 Router 的 Softmax 输出值进行均值与箱线图统计。
3. **[Exp-4.X] 鲁棒性扰动实验 (JPEG/Resize)**
   - **代码修改**：写一个专用的扰动测试脚本。虽然可以使用离线特征，但由于是对原图进行 JPEG 压缩等变换，**不能再使用离线特征 (`.pt`)，必须重新走一遍带 CLIP 的原图解码推理流程**（但仅做测试，无需训练，因此耗时也可以接受）。

---
**下一步行动建议**：
建议优先修改模型代码，把 `[Exp-1.1, 1.2, 1.3]` 这三个依赖重新训练的网络结构跑完。随后进入纯推理解图表阶段。