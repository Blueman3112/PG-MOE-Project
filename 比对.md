# 规划0505.md 与 规划0420.md 的核心差异比对

## 1. 实验数据集层面的收敛
- **0420版本**：提出在 Dataset-A 和 Dataset-B 上进行交叉测试以证明泛化性。
- **0505版本**：明确**摒弃 Dataset-A** 作为主实验指标，要求所有核心实验完全收敛、统一在 `dataset-B` 上进行，以防止对比图表过于冗杂，弱化实验焦点。

## 2. 实验设计的针对性修正
- **单模态对照基线 (Exp 1.1 / 1.2)**：
  - **0420版本**：建议采用“双空间专家”或“双频域专家”。
  - **0505版本**：明确**否决**了双同源专家的设计，因为这会额外增加参数量，引入了“性能变化是因为模态互补还是单纯参数变多”的混淆变量。改为纯粹的 `Spatial only` (仅单空间) 和 `Frequency only` (仅单频域)。
- **融合机制新增 (Exp 1.3)**：
  - **0505版本**：明确补充了 `Add Fusion` (等权相加融合) 的具体实验要求。

## 3. 指标与写作导向的规范化
- **统计指标**：0505版本给出了强制性的五维指标列表（Acc, AUC, F1, Precision, Recall），并钦定 AUC 为主指标。
- **表述降温 (Tonal Restraint)**：0420版本的措辞较为激进（如“彻底实现特征解耦”）。0505版本特别强调**结论写实**，要求论文表述降温维稳（如改为“正交约束降低了专家分支之间的特征冗余”），更加符合学术客观严谨性。
- **章节映射**：0505版本不再按抽象维度划分，而是**直指最终论文第五章的子章节结构**（如 5.2 主实验、5.3 融合对比等），从“探索期”转变为“成文期”。

## 4. 可解释性的量化细则
- **权重统计**：0505版本为权重（Routing weights）统计指明了具体的对比组方案（方案A：Real vs GAN vs Diff，或方案B：Real vs Correct Fake vs Wrong Fake），直接提供了图表骨架。